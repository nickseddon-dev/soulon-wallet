# Task 5 联调与门禁可审计记录

- 执行时间: 2026-03-04
- 执行范围: 钱包前端（wallet-app）与后端 API（soulon-backend）联调、质量门禁校验
- 对应任务: `launch-wallet-mainline-development/tasks.md` Task 5

## 联调结果

- 后端服务启动验证: `go run ./cmd/api` 成功启动，监听 `:8082`
- 健康接口验证: `GET /v1/health` 返回 `{"status":"ok"}`
- 链状态接口验证: `GET /v1/indexer/state` 返回结构 `{"reorgs":0,"tipHash":"","tipHeight":0,"total":0}`
- 事件接口验证: `GET /v1/indexer/events?limit=2&offset=0` 返回结构 `{"events":[],"total":0}`

## 质量门禁结果

- 钱包前端门禁: 在 `wallet-app` 执行 `npm run validate` 通过（包含 lint、typecheck、build）
- 后端门禁: 在 `soulon-backend` 执行 `go test ./...` 通过
- 联调集成门禁: 在 `soulon-backend` 执行 `.\scripts\run-integration.ps1` 通过（`TestKafkaAndPostgresIntegration` = `PASS`）
- 诊断检查: 工作区诊断为 0 条错误

## 前端校验结果

- 启动验证: `npm run dev -- --host 127.0.0.1 --port 5173` 正常启动
- 路由可访问性:
  - `GET /` => `200`
  - `GET /state` => `200`
  - `GET /events` => `200`
  - `GET /login` => `200`

## 阻塞状态

- 阻塞项: 钱包默认 API 基址为 `http://127.0.0.1:8080`，后端默认监听为 `:8082`，存在默认联调端口不一致
- 影响: 新环境未创建 `.env` 时，钱包默认请求会命中错误端口导致联调失败
- 处理: 将钱包默认 API 基址和 `.env.example` 同步调整为 `http://127.0.0.1:8082`
- 当前状态: 已解除，无剩余阻塞项

## 变更清单

- `wallet-app/src/config/env.ts`
- `wallet-app/.env.example`
- `soulon-backend/.trae/specs/launch-wallet-mainline-development/tasks.md`
